# Project Status
status: active
summary: Round 3 Babylon.js spike end-to-end — 14 of 18 plan tasks shipped across 12 commits on master during this session. `/babylon-spike` route renders a procedural-tile iso village (6 cottages at cluster centroids, grass + dirt-path tiles, UV-scrolling brook, forge smoke plume, hover+click+rotation wired, directional-light shadows) without console errors. Round 2 `/phaser-spike` stays live for side-by-side comparison, Round 1 `/spike-isometric` stays for historical reference, `/` is still the live Leaflet hero at https://mushroomgreen.uk. Remaining spike tasks: visual verification of rotation/hover/click (needs Tom's keyboard + mouse) and the side-by-side gate screenshots at `/spike/comparison/`. Scene aesthetic is knowingly behind the Townscaper north-star by a wide margin — procedural materials are flat speckled patterns, not painted brush-stroke surfaces. Spike's job is "does the engine pivot clear the Phaser ceiling"; gate decision pending Tom's review.
needs-human:
  - Click "Allow" on the Chrome accessibility permission dialog blocking browser-harness (was triggered while trying to capture side-by-side screenshots for Task 18)
  - Visual review of /babylon-spike at http://localhost:4323/babylon-spike — rotate with arrow keys, hover cottages, click through to /households/[id]/
  - Gate decision at Task 18: "clears the Phaser ceiling → proceed to production plan" vs "specific gaps remain → iterate in spike"
  - Confirm it's OK that we pivoted cottages/props/tiles from glTF import to procedural meshes. Blender recipes for tile-grass + tile-dirt-path stay committed as swap-outs; cottage/prop Blender scripts skipped in favour of procedural pending Townscaper-level aesthetic budget
  - (from previous session — still applicable) Decide whether to un-gitignore public/spike-sprites/ so /phaser-spike works on netlify.app
next-for-claude:
  - Once Chrome permission is approved, run the browser-harness one-liner in spike/comparison/babylon-vs-phaser.md to capture /babylon-spike default + 4 rotations + /phaser-spike default; fill the Observations section with the real comparison
  - Address whichever gaps Tom flags at the gate. Likely candidates: camera zooms in to clusters only instead of boundary, Blender-authored meshes for cottages with proper brick normals + texture, brook routing with dual-graph placement (on tile edges, not tile faces), tile set expansion (paving, terrace stones, grass-to-water)
  - Lighthouse pass on the /households/[id] detail pages (deferred from Round 2)
blocked-by:
  - Chrome accessibility permission dialog needs Tom's click before browser-harness can drive the new Chrome instance
sub-projects:
  - name: spike/
    summary: Three-round spike — Leaflet overlay (round 1, ceiling hit) → Phaser 2.5D village (round 2, ceiling cleared but ran out of runway aesthetically) → Babylon.js 3D iso (round 3, 14/18 tasks, gate pending). Round 3 pivoted from Blender glTF to procedural meshes after hitting five Babylon 8.x modular-import gotchas; all documented in memory (feedback_babylon_modular_gotchas.md) + spike/comparison/babylon-vs-phaser.md
    needs-human: See root needs-human
    next-for-claude: See root next-for-claude
