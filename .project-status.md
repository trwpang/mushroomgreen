# Project Status
status: active
summary: Phase C done — Victorian page chrome wired. IM Fell English loaded in BaseLayout and set as primary body font in global.css (Georgia fallback); index.astro header bumped 48→72px with 1.2em title / 0.8em subtitle and overrides VillageMap's inline #map height to calc(100vh-72px) via :global selector. Four new fixed-position chrome components added: VictorianTitleCartouche (bottom-left nested-bordered cream cartouche), VictorianBorder (3px outer frame + four cross-pattée corner SVGs), ScaleOfChains (4-segment chain bar bottom-right), SurveyedBy (italic surveyor attribution). All four are pointer-events:none and hidden in @media print. Build + astro check clean.
needs-human:
  - Visual side-by-side check at http://localhost:4321/ vs legacy/Map/mushroom_green_1865_v10_11_35_28.html (especially: cartouche position vs compass, scale-bar alignment, IM Fell English font loaded)
  - Print preview (Cmd+P) check — chrome should be hidden, header+map should fill the page
  - Decide Netlify subdomain name at first deploy
  - Configure DNS at registrar to point mushroomgreen.uk → Netlify
  - Approve preview deploys before merging father-supplied content
next-for-claude:
  - Build the per-household page template and inset map component
  - Set up netlify.toml and first deploy
blocked-by:
