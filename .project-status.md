# Project Status
status: active
summary: MVP map LIVE at https://mushroomgreen.uk. Per-household detail pages now shipped at /households/{id}/ (all 59). New painterly 2.5D Phaser village at /phaser-spike — spike-only but fully functional: hand-rolled iso projection, meadow + brook + dirt paths, 14 clustered cottage/forge/bridge/willow sprites with y-sort, forge smoke particle emitters, mouse pan/zoom, hover tooltips, click → /households/{id}/ detail page. Closes the "decorated Leaflet" ceiling hit by /spike-isometric. Dev-only until sprites un-gitignored.
needs-human:
  - Visual review of /phaser-spike default view + zoomed forge shot — confirm it WOWs before promoting toward a production hero
  - Decide whether to un-gitignore public/spike-sprites/ (force-add the PNGs) so /phaser-spike works on netlify.app, or keep dev-only
  - Decide whether to replace / (Leaflet hero) with /phaser-spike or keep both as alternate experiential entry points
  - Confirm father's preferred credit name on the about page (when built)
next-for-claude:
  - If promoting /phaser-spike: add mobile touch-gesture pan/zoom, zoom-reactive particle scale, proper brook/road routing so the bridge has a literal crossing, single-willow sprite variants
  - Stretch: animated brook flow (UV-scroll shader), willow sway, sprite atlas packing
  - Lighthouse pass on the new /households/[id] detail pages
blocked-by:
sub-projects:
  - name: spike/
    summary: Two-round spike — Leaflet overlay (round 1, hit ceiling) → Phaser 2.5D village (round 2, ceiling cleared). All assets under gitignore; prompts reproducible in spike/prompts.md
    needs-human: Review /phaser-spike at default + zoom; decide on ship vs refine
    next-for-claude: See "next-for-claude" above
