# Project Status
status: active
summary: Task 15 done — src/components/VillageMap.astro ports the legacy Leaflet map (inverse mask, settlement boundary, Black Brook, two greens, compass control, all 59 polygons fetched from /households.json, hover popups with View household links, founder pulse + bio for Henry Weaver). Build + astro check clean. index.astro left as the placeholder for Task 16.
needs-human:
  - Decide Netlify subdomain name at first deploy
  - Configure DNS at registrar to point mushroomgreen.uk → Netlify
  - Approve preview deploys before merging father-supplied content
next-for-claude:
  - Task 16: Replace placeholder src/pages/index.astro with the real header (title bar + legend) wrapping <VillageMap />, including the @media print CSS block from the legacy HTML
  - Build the per-household page template and inset map component
  - Set up netlify.toml and first deploy
blocked-by:
