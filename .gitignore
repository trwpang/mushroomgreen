# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Local Netlify folder
.netlify

# Sprite spike — local only, not deployed
spike/sprites/raw/
spike/sprites/keyed/
public/spike-sprites/

# Brainstorming session files
.superpowers/

# Babylon spike assets — local only, not deployed
public/babylon-sprites/

# Forge study: reference conversions, disposable logs, Blender backup saves
artifacts/forge/reference/
artifacts/forge/*.log
assets/forge/*.blend1

# Village build artifacts
artifacts/village/*.log
assets/village/*.blend1

# Python build caches and local agent checkouts
__pycache__/
*.pyc
.claude/worktrees/

# Unvalidated Blender exports never enter the served public directory
artifacts/forge/raw/
artifacts/village/raw/

# Chainmaker study: staging, disposable logs, private reference capture and Blender backups
artifacts/chainmaker/raw/
artifacts/chainmaker/*.log
artifacts/chainmaker/reference-browser.png
assets/chainmaker/*.blend1

# Reproducible Tripo inspection intermediates (keep the source, Blender repair, and runtime GLB)
assets/chainmaker/tripo-v2/*.blend1
assets/chainmaker/tripo-v2/inspection.blend
assets/chainmaker/tripo-v2/static-preview*.glb
assets/chainmaker/tripo-v2/recovered-rig.glb

# Far-country map tiles are a reproducible download cache
artifacts/village/far-country/tiles/
