import * as T from 'three';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';import {MeshoptDecoder} from 'three/addons/libs/meshopt_decoder.module.js';import {RoomEnvironment} from 'three/addons/environments/RoomEnvironment.js';
const r=new T.WebGLRenderer({antialias:true});r.setSize(1200,600);r.setPixelRatio(1);r.setClearColor('#c6c0b3');r.toneMapping=T.ACESFilmicToneMapping;document.body.style.margin='0';document.body.appendChild(r.domElement);
const s=new T.Scene(),p=new T.PMREMGenerator(r),e=new RoomEnvironment();s.environment=p.fromScene(e).texture;s.add(new T.HemisphereLight(0xffffff,0x554633,2));const sun=new T.DirectionalLight(0xfff0d9,3);sun.position.set(1,2,2);s.add(sun);const g=await new GLTFLoader().setMeshoptDecoder(MeshoptDecoder).loadAsync('/henry-statue/henry.glb');s.add(g.scene);const c=new T.PerspectiveCamera(36,2/3,.01,20);r.setScissorTest(true);for(let i=0;i<3;i++){const a=Math.PI*.5+i*Math.PI*.5;c.position.set(Math.sin(a)*.85,.28,Math.cos(a)*.85);c.lookAt(0,.23,0);r.setViewport(i*400,0,400,600);r.setScissor(i*400,0,400,600);r.render(s,c);}document.body.dataset.ready='true';
